[build]
  base    = "fan-tribute-frontend"
  command = "npm run build"
  publish = "dist/fan-tribute-frontend/browser"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS    = "--legacy-peer-deps"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
